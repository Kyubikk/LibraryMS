namespace LoginSystemApp
{
    public class SharedResource
    {
        // Lớp này không cần nội dung gì cả.
        // Nó chỉ được sử dụng làm "marker" để xác định nguồn tài nguyên (resource).
    }
}
